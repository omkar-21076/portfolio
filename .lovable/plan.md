Add `roleDescription` to the Compliance Companion case study meta object in `src/routes/work.compliance-companion.tsx`, using the exact text provided by the user. The `CaseStudy` component already supports this prop, so only the case study route file needs updating.